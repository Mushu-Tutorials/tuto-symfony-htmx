<nav class="bg-gray-800">
  <div class="mx-auto max-w-7xl px-2 sm:px-6 lg:px-8">
    <div class="relative flex h-16 items-center justify-between">
      <div class="flex flex-1 items-center justify-center sm:items-stretch sm:justify-start">
        <div class="">
          <div class="flex space-x-4">
            <a href="{{ path('app_spa') }}" class="bg-gray-900 text-white px-3 py-2 rounded-md text-sm font-medium" aria-current="page">Accueil</a>
            <a hx-target="#response-div" hx-get="{{ path('app_spa_features') }}" hx-swap="outerHTML" href="#" class="text-gray-300 hover:bg-gray-700 hover:text-white px-3 py-2 rounded-md text-sm font-medium">
              Features
            </a>
            <a hx-target="#response-div" hx-get="{{ path('app_spa_contact') }}" hx-swap="outerHTML" href="#" class="text-gray-300 hover:bg-gray-700 hover:text-white px-3 py-2 rounded-md text-sm font-medium">
              Contact Form
            </a>
            <a href="{{ path('app_htmx') }}" class="text-gray-300 hover:bg-gray-700 hover:text-white px-3 py-2 rounded-md text-sm font-medium">
              HTMX
            </a>
            <a hx-target="#htmx-div" hx-get="{{ path('app_htmx_click_to_edit') }}" hx-swap="outerHTML" href="#" class="text-gray-300 hover:bg-gray-700 hover:text-white px-3 py-2 rounded-md text-sm font-medium">
              Click to edit
            </a>
          </div>
        </div>
      </div>
    </div>
  </div>
</nav>
